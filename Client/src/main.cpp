#include <GLFW/glfw3.h>
#include <spdlog/spdlog.h>

#include <fmt/format.h>
#include <vulkan/vulkan.hpp>
import Application;
import system;
import types;

int main( int, char ** ) {
	sys::Console::INIT( );

	return 0;
}
